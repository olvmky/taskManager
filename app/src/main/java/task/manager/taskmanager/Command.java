package task.manager.taskmanager;

/**
 * The Command interface represents a command that can be executed.
 * Classes that implement this interface will provide the logic for the command's execution.
 */
public interface Command {
    void execute();
}
